{
  "name": "railroad-device-monitoring-dashboard",
  "version": "1.0.0",
  "scripts": {
    "start:api": "cd api && npm start",
    "start:app": "cd app && npm start",
    "start:project": "concurrently \"npm run start:api\" \"npm run start:app\""
  },
  "devDependencies": {
    "concurrently": "^8.2.1"
  }
}
